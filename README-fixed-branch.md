# `fixed` Branch

This branch contains fixes for resource leaks (see main branch for workshop bugs).

## Changelog
- v1.0.5-fixed: Try-with-resources and error handling improvements for connection leaks in `OrderService.java`
